            {
            if(arr[i]==arr[i-1])
        {
        for(int  i=1;i<arr.length;i++)
        int max=0;
        Arrays.sort(arr);
        int c=1;

    public int longestConsecutive(int[] arr) {
class Solution {
        if(arr.length==1) return 1;
